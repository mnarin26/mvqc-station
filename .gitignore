# Python
.venv/
__pycache__/
*.pyc

# Runtime state (SSD-1 / eMMC)
database/*.db
database/*.db-wal
database/*.db-shm
logs/*.log*
cache/

# Frontend
app/frontend/node_modules/
app/frontend/dist/

# Training server data + artifacts
training-server/data/
training-server/registry/
training-server/**/_ingest_tmp/

# Built frontend (regenerated by `npm run build`)
# app/station/web/   # keep the vanilla fallback under version control

# Deploy yedekleri
*.bak*
